Add a new menu item "وساطه AI اكاديمي" below the "مساحة العمل" item in the right sidebar navigation (RightSliderComplete.tsx). On tap, it opens `https://training.wasataai.com` in a new tab using the ExternalLink icon with the brand gold color (#D4AF37). No database or backend changes needed.