/**
 * SlugQuotePage.tsx
 * صفحة إرسال طلب عرض السعر - تستخدم المكون الموحد
 */

import PublicPriceQuoteForm from '@/pages/public-forms/PublicPriceQuoteForm';

const SlugQuotePage = () => {
  // نستخدم المكون المحسن PublicPriceQuoteForm مباشرة
  return <PublicPriceQuoteForm />;
};

export default SlugQuotePage;
