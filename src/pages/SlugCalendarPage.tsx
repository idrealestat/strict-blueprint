/**
 * SlugCalendarPage.tsx
 * صفحة إنشاء موعد - تستخدم المكون الموحد
 */

import PublicAppointmentForm from '@/pages/public-forms/PublicAppointmentForm';

const SlugCalendarPage = () => {
  // نستخدم المكون المحسن PublicAppointmentForm مباشرة
  return <PublicAppointmentForm />;
};

export default SlugCalendarPage;
