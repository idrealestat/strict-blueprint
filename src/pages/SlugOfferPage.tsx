/**
 * SlugOfferPage.tsx
 * صفحة إرسال عرض عقاري - تستخدم المكون الموحد
 */

import PublicOfferForm from '@/pages/public-forms/PublicOfferForm';

const SlugOfferPage = () => {
  // نستخدم المكون المحسن PublicOfferForm مباشرة
  return <PublicOfferForm />;
};

export default SlugOfferPage;
